# Django Core
Django>=5.0,<6.0
python-dotenv>=1.0.0
setuptools>=65.0.0  # Required by rest_framework_simplejwt

# Database
psycopg[binary]>=3.1.0  # PostgreSQL adapter

# Django REST Framework & Authentication
djangorestframework>=3.14.0,<4.0
djangorestframework-simplejwt>=5.3.0

# Social Authentication
dj-rest-auth>=5.0.0  # REST authentication with social auth support
django-allauth>=0.57.0  # Social authentication (Google, Facebook, GitHub, etc.)
cryptography>=41.0.0  # Required by django-allauth for JWT/OAuth
PyJWT>=2.8.0  # JWT encoding/decoding for social auth

# Email & Phone Verification
django-phonenumber-field>=7.2.0  # Phone number validation
phonenumbers>=8.13.0  # Phone number parsing library

# Multi-Factor Authentication
pyotp>=2.9.0  # TOTP/HOTP implementation
qrcode>=7.4.0  # QR code generation for TOTP setup

# Role & Permissions Management
django-guardian>=2.4.0  # Object-level permissions (Rolify equivalent)
rules>=3.3  # Rule-based permissions

# CORS & Security
django-cors-headers>=4.3.0

# Filtering & Search
django-filter>=23.5

# Image Processing
Pillow>=10.0.0

# Geospatial (PostGIS)
# Will add after PostgreSQL is set up

# Background Tasks (Celery)
celery>=5.3.0,<6.0
redis>=5.0.0
django-celery-beat>=2.5.0  # Periodic task scheduler
django-redis>=5.4.0  # Redis cache backend

# SMS (Hubtel for Ghana)
requests>=2.31.0  # HTTP library for API calls

# Google APIs (AdSense)
google-auth>=2.0.0  # Google authentication
google-auth-oauthlib>=1.0.0  # OAuth flow for Google APIs
google-api-python-client>=2.0.0  # Google API client

# Testing
pytest>=7.4.0
pytest-django>=4.7.0
pytest-cov>=4.1.0

# Export Libraries (Excel & PDF)
openpyxl>=3.1.0  # Excel file generation
reportlab>=4.0.0  # PDF generation

# Production Server
gunicorn>=21.0.0

# Code Quality
flake8>=6.1.0
black>=23.12.0

# Utilities
python-decouple>=3.8
